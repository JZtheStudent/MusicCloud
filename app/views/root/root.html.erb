<% if logged_in? %>
  <script type="text/javascript">
    console.log('Signed in')
    window.currentUser = <%= render(
      "api/users/user.json.jbuilder",
      user: current_user
    ).html_safe %>
    console.log(currentUser.id)
  </script>
<% end %>

<div id="root">
  <h1>React don't work dawg</h1>
</div>
