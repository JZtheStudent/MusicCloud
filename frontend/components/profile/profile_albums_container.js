import { connect } from "react-redux";
import ProfileAlbums from "./profile_albums";

const mapStateToProps = state => ({

});

const mapDispatchToProps = dispatch => ({

});

export default connect(mapStateToProps)(ProfileAlbums);
