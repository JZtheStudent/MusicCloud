import { connect } from "react-redux";
import ProfilePlaylists from "./profile_playlists";

const mapStateToProps = state => ({

});

const mapDispatchToProps = dispatch => ({

});

export default connect(mapStateToProps)(ProfilePlaylists);
